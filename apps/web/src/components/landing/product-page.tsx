import { ProofLanding } from '@/components/landing/proof/proof-landing';

/**
 * Marketing homepage — art-directed proof dossier experience.
 * Application routes (/demo, /demo/card, dashboard) are untouched.
 */
export function ProductPage() {
  return <ProofLanding />;
}
